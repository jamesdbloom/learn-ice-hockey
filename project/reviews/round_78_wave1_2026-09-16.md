# Round 78 — release-one wave 1, 16 September 2026

Eight agents on disjoint file sets, dispatched from
[`baseline_2026-09-16_workstream_0.md`](baseline_2026-09-16_workstream_0.md) and
[`worklists_2026-09-16.md`](worklists_2026-09-16.md), both derived fresh at
`734d8b0` on a clean tree with no agent live.

Every brief carried the standing instruction to **refute its own premises before
acting**, named its exclusive file set, forbade builds and shared-tool edits, and
**specified the defect and the constraint rather than replacement wording** —
`CLAUDE.md`'s round-73 rule, after a coordinator once specified a sentence that
would have made a document contradict itself two paragraphs above.

## Ownership, as dispatched

| Agent | Owned exclusively |
|---|---|
| readability pilot | `content/foundation/rules_primer.md` |
| readability pilot | `content/getting-started/getting_started.md` |
| principles audit | `content/foundation/core_principles.md` |
| rules residual | `content/foundation/uk_rules.md` |
| rules residual | `content/positions/defender.md` |
| markers + apparatus | `content/technique/body_contact_and_battles.md` |
| rules verification | **read-only**, edited nothing |
| zone polygons | `site/src/diagrams/` (whole directory) |

The diagram directory went to **one** agent rather than several because diagram
ids are borrowed across up to ten modules, so no smaller partition of it is
disjoint. `CLAUDE.md` records four agents being sent into that directory in one
round, each brief asserting exclusivity, and an agent's rebuild then publishing
six captions it had never read under its own clean checker run.

## ⚠️ Three briefs were wrong, and the instruction to refute them is what caught it

**1 · The rejected-candidate log does not exist in `content/`.** The
`core_principles.md` brief told its agent *"do not delete a rejected-candidate
entry — it is the document's integrity story."* There is no such log in that
file. It lives in `project/`. ⚠️ **Had the agent obliged, a rejected-candidate log
inside `content/` is the corpus narrating its own review process — non-negotiable
6. The brief would have instructed a breach.** The cause was
`READABILITY_AND_DUAL_AUDIENCE.md`'s phrase *"the index's own rejected-candidate
log"*, where *"the index"* meant the index **workstream**. That line has been
rewritten to name the files and to say plainly that such a log must never sit in
`content/`.

**2 · The `rules_primer.md` Key Takeaways figure was inflated 3×.** See
[`census_trailer_misattribution_2026-09-16.md`](census_trailer_misattribution_2026-09-16.md).
The brief said 15,588 words and *"the largest single thing you could cut"*; the
agent measured **5,156** and reported the brief. The tool attributes every
document's unheaded Sources trailer to the preceding heading. **195,639 words
corpus-wide.** The corpus summary layer is **12.2%, not the 29.5% every plan
document asserts.**

**3 · The shorthanded-icing cluster was stale.** The `rules-verifier` read NHL
81.6 and 20.3, IIHF 81.6 in both editions, USA Hockey 624, Hockey Canada 6.7 and
CARHA 65 end to end, and reported that commits `862c0dd` and `8af85e4` had
already closed the substance. It tested **seven exclusivity and silence claims
and all seven held.** ⚠️ **That is a departure from the round-59 pattern, where
every absence claim tested was false**, and it is worth recording as such: these
were written with the casebook already read.

## Two coordinator errors, retracted in place

**`rules_primer.md:815` holds the tightest table in the corpus** — 0 rows and 66
characters of headroom. The baseline said that table *"does not appear in today's
`--near` output at all"*, because I read only the **tail** of the output. The
plan's line number was six lines stale; **its substance was right and I called it
stale.**

⚠️⚠️ **AND IT WAS NOT ONE BRIEF. The same incomplete read corrupted TWO.** The
`getting_started.md` brief was told its two tables at `:139` and `:541` were no
longer near the limit; they are at `:150` and `:552`, one of them at 2 rows of
headroom. **The tool prints nine near-limit tables; I had seen five.** ⚠️ **An
absence found by an incomplete read is not an absence** — the same shape as a
zero grep on an image-only PDF, now recorded five times in this repository. Both
agents re-derived and reported me, which is the *"refute the brief"* instruction
doing precisely the job it exists for.

**The missing `.m4a` files were not a finding.** The audio is gitignored and
uploaded out of band; `podcast.json`'s own `note` says so. An absence in a working
tree is not an absence in the product.

Both retractions stand in the records that carried the errors rather than being
silently corrected, because the wrong figure is part of the evidence.

## What changed in `content/`

| File | Result |
|---|---|
| `foundation/rules_primer.md` | Page-opening principle added (drawn from and linked to core principle 2). Key Takeaway 9 reduced 1,257 → 1,181 by replacing a clause-by-clause re-derivation of Rule 69 with its consequence, keeping the hedge and the referee's-judgement counterweight verbatim. A duplicated IIHF 44.1 quotation removed from inside one table row. **4 of 6 old-side markers moved.** Net **+39 words** — the framing principle cost more than the apparatus saved. |
| `technique/body_contact_and_battles.md` | **8 of 8 markers moved; the document now reads `already complies`.** −321 words, citation share 55.7% → 55.2%. Four apparatus cuts, each routing to an owning section in the same document. Page principle added naming **two** of the seven, because two govern opposite halves of the page. |
| `foundation/uk_rules.md` | **A complete alternative playing code for U12 was missing from the corpus.** IHUK Appendix D — no icing, no offside stoppage, running clock, line-locked shifts, 3- or 4-minute minors, no coincidental penalties — existed in the corpus only as the unexplained phrase *"Full ice, non-checking, running clock"* inside a Sources trailer that is never voiced. Now in six layers. |
| `foundation/core_principles.md` | A misattributed verbatim quotation repaired: the document credited **both** the NHL and the IIHF with Rule 56.1's wording; the NHL writes it in the **singular**. The document asserts two lines later that *"every quotation above is the governing document's own wording"*, so it breached the property it claims. Principle 2's missing recognition cue added. |
| `positions/defender.md` | A ` ```facts ` line said **all four books** gloss the centre-red-line rule as *"checking an opponent counts, not just playing the puck."* **Only the NHL and IIHF write that gloss**; USA Hockey 407(c) and Hockey Canada 4.13(c) write an intent word and none. ⚠️ **The body had it exactly right at `:143`. The extraction layer — voiced alone with a 300 ms break either side — did not.** Also: a superseded In-House Rules edition named; England Ice Hockey R&R quoted in four places with **no Sources entry at all**; CARHA added as the one adult-recreational book held, with its *"PRINTED 2020"* and affiliated-leagues-only qualifications. |
| `getting-started/getting_started.md` | Page principle added from core principle 2. **The document's top-level contact instruction told a beginner to ask the rink desk whether there is contact — right for a drop-in, wrong for a league.** Separated and scoped, verified against USA Hockey 604(a) and both IHUK RoCs. −476 words, of which 356 were two trailing italic paragraphs that were **voiced, and were the last thing a listener heard in the corpus's entry-point document**. |
| `systems/special_teams.md` + `foundation/rules_primer.md` | The briefed over-scope repaired at `:22` and `:682`. ⚠️ **But the two real finds were not in the brief** — see below. |

## ⚠️ A class of defect no checker in this repository can see

**`check_absolutes.py` scores penalty TIERS and DENIALS. A PERMISSION over-scoped
by age, class or country is invisible to it.** It passed on both of these, before
and after repair. Both were found by an agent reading a document it had been sent
to for something else.

- `rules_primer.md` — *"get the clear away before the clock runs out **and it is
  legal wherever you play**"*. Its own voiced paragraph, **35 lines from the
  Exception that qualifies it.** Below USA Hockey's classification limit there is
  no exemption at all: that clear comes back for a defensive-zone draw.
- `rules_primer.md` — *"still free to ice the puck, **in all five books**"*. Same
  shape, and **bolded**, which is where a scanning reader stops.

⚠️ **The corpus-wide census for this shape was started and NOT completed**, and is
a plan row. The pattern is dominated by the standing `> **Rule set:**` banner,
which is correctly scoped in all 39 instances — and
`language_and_glossary.md`'s `Shorthanded` entry scores on it while being
**exemplary**, carrying the classification limb, the NHL carve-out, the head-count
test and the IIHF's silence-with-a-reason. **This pattern must not be swept.**

## ⚠️ A cross-document staleness created INSIDE this round

`rules_primer.md` asserted that `uk_rules.md` *"does not carry the U12 competition
format"*. **That was true in the morning and false in the afternoon** — the
`uk_rules.md` agent added exactly that section during the same wave. Caught and
repaired by a later agent in the same round.

**This is the hazard of wide parallelism stated precisely:** a claim one agent
makes *about another document* is true only of the revision it read, and no
checker compares the two. The defence is the ownership table plus a late agent
that re-reads — not the checkers.

## ⚠️ A renderer trap that no checker sees, now in the style guide

`md_to_speech.py` expands `U14` to *"under fourteen"*. So a preposition in front of
an age token **doubles the word**: an agent's draft *"below U14 IHUK's junior
formats change the game"* voiced as **"below under fourteen."** It rewrote it to
*"at U12 and U10"*, which names the same set and voices cleanly.

**Every gate passes on the doubled form** — it is legal Markdown and a legal fact.
**It was caught only by rendering the sentence.**

## What changed outside `content/` — coordinator only

**`site/src/diagrams/rules_primer.mjs` — `icing-gaining-the-line`'s
`your own half` polygon.** It stopped **8.5 ft short of both side boards** and
stopped dead at the goal line, never reaching the end boards, with a **dashed
stroke drawn along that false inset**. The faceoff circles visibly poked out of
it. It contradicted the rule — the half is the half, boards to boards and behind
your own net — and it contradicted **the diagram's own `describe`**, which says
*"from the centre red line back to the end boards."*

⚠️ **`check_zones.py` could not have found it.** All three of its passes compare
diagrams to each other, and `your own half` has a single instance in the corpus:
nothing to pair on label, nothing on IoU, nothing on span. It was found by an
agent auditing every board-touching zone in the directory and noticing this was
the sole outlier on **both** counts — reaching the boards, and carrying a stroke.

Repaired by the coordinator because the agent could not: the renderer sites a
zone label at the polygon's **vertex mean**, the corrected polygon samples the
board arc at 1 ft, and the mean moves from x −61.67 to x −83.13 — far enough that
a 13-character label runs past the end boards. It needed a `labelDx` **chosen and
then seen**, and the agent was forbidden to build. Verified after rebuild:
polygon now spans x −100…0 and y ±42.5, `stroke="none"`, label restored to
**x −61.67, identical to before**.

⚠️ **And the repair made the region visible to the tool that had been blind to
it:** `check_zones` now pairs `your own half` with `defending zone` at **75% of
the same ice** — which is the arithmetic (a 75 ft zone inside a 100 ft half) and
confirms the geometry independently.

`boardArc` was **exported** from `risk_management.mjs` rather than copied a third
time. Its own comment forbade the third copy but gave two false reasons: that *"no
diagram module imports anything"* (`offensive_zone_play.mjs` and
`playing_without_the_puck.mjs` both do) and that the fix was to export the
**first** copy (which is one-argument and reaches the near end only). Both
corrected in place.

**`site/src/diagrams/risk_management.mjs` — a scope clause restored.** Commit
`d09b318` shortened `the-risk-map`'s caption and cut the clause scoping which ice
the *"their end"* grade applies to. The zone comment still asserted the over-reach
was *"disclosed in the caption rather than silently included"*; for one commit it
was not. Restored in the cut version's own words.

**`site/src/diagrams/rink_map_and_glossary.mjs`** — `the-high-slot` and
`the-low-slot` `describe` fields said *"the upper half"* and *"the lower half"*.
The owner says *"part"*, and the drawing contradicts *"half"*: 210 sq ft against
196. The same file had already settled this principle for a neighbouring diagram
and not applied it here.

**`CLAUDE.md` and `scripts/check_zones.py` — a phantom owner document.**
`content/foundation/rink_map_and_glossary.md` has not existed since commit
`fd9e903` split it into `rink_map.md` and `language_and_glossary.md`. It is still
named by four scripts' comments and 21 diagram modules, and ⚠️ **no `owner:` field
carries it, so `check_links.py` cannot see any of them.** An agent told *"only
`rink_map_and_glossary.md` settles what a region is"* greps for a file that is not
there — the routing failure `CLAUDE.md` warns about for sources, reproduced for
the corpus's own owner document. ⚠️ **And the split means there are now TWO
owners:** regions are `rink_map.md`'s, vocabulary is `language_and_glossary.md`'s.

**`scripts/check_zones.py`'s docstring said the glossary defines *"the middle"* as
a DIRECTION.** It defines it as *"the central lane"* (`rink_map.md:483`). ⚠️ **A
lane has a width and no stated length** — so the four polygons labelled *the
middle* are right to agree on width and free to differ in length, which is what
they do. **The old wording would have licensed width divergence, the one
dimension they must not differ in**, and the error had already propagated into
`risk_management.mjs`'s comment. Docstring only; no output changed.

⚠️ `CLAUDE.md`'s `check_tables` passage has now drifted **twice in the same
paragraph** — it named `body_contact_and_battles.md:55` (now `:60`, two rows
spare), was repaired to name `rules_primer.md:753`, and that line is not in
`--near` either. **The repair failed the same way the thing it repaired did, under
a sentence reading "NO COUNT IS WRITTEN HERE. RUN THE TOOL."** ⚠️ **A line number
is a figure.** No file, line or count for a near-limit table will be written there
again.

## The `safety-reviewer` pass

Four documents, read against primary text in eight rulebooks, with the spoken
layer rendered through `md_to_speech` rather than reasoned about from the
Markdown.

**Verdicts:** `body_contact_and_battles.md` **safe** · `uk_rules.md` **safe** ·
`core_principles.md` **safe** · `getting_started.md` **defect found — two
Majors**. **No Critical established.**

### ⚠️ Both Majors are in text written today, and both run permissive

They were plan rows in `OPEN_ITEMS.md` Workstream 1E; **that workstream is now
CLOSED** — both were repaired and the repairs are in this commit. In
short: the entry-point document's **first substantive spoken paragraph** now puts
*"the gear requirement"* on the list of things *"set by your club, your
association or your rink"* — and protective-equipment floors are set by the
rulebook, carry penalties, and in Britain are enforced *"No warning will be
given… an immediate 10-minute Misconduct"*. And a helmet warning covering two
named learn-to-play programmes was retired on the premise that their kit is
*"given to you to keep, and it is new"* — **a premise with no citation anywhere
in the corpus**, which then propagated cleanly into Common Mistakes and Key
Takeaway 2.

⚠️ **Both authors reported their own work as sound. This is what "do not review
your own writing" is for**, and it is why the same defect could survive eight
rounds and fall in the ninth.

⚠️ **The reviewer explicitly declined to call the second one false** — it
reported that a safety warning was withdrawn on an unsourced premise, not that
the premise is wrong, and said only a network fetch settles it. That distinction
is the correct one and is preserved in the plan row.

### What the pass verified rather than assumed

**All four `body_contact_and_battles.md` cuts were traced to their destinations
and found present in every layer**, including the facts layer, so they survive
extraction. The reviewer tested the case the brief asked for — *the reader who
meets only the compressed Overview and never reaches §5* — and found that reader
is told the boarding onus is absolute and never told a softening exists. **That is
the safe failure mode.** The routed penalty tier, which is the edit
`check_absolutes.py` exists to catch, was **read rather than trusted to the
pass**, and verified against `usah.txt` and `hc_layout.txt`.

⚠️ **The new negative-existence claim in `uk_rules.md` was re-tested by the
reviewer running the search itself** — six extractions, flattened — and **it
holds**. Total icing/offside hits outside Appendix D: one in the U10 RoC and one
in England Ice Hockey's R&R, **both the registration sense** (*"icing an
ineligible player"*), and one in the In-House Rules, Rule 76's *"face-offs
following icings"*. The disclosure is true, the method in its Notes on
verification is the method that was used, and it names the right sole exception.

**Every U12 Appendix D quotation was verified verbatim** — fourteen provisions,
from *"No icing calls at U12"* to *"Coincidental penalties are not applied at
U12"*. **The `core_principles.md` Rule 56.1 repair was verified correct against
both books**: NHL *"the ice **he** is standing on… **is** not required to move"*,
IIHF *"the ice **they** are standing on… **are** not required to move"*.

⚠️ **And the pass was run deliberately in both directions.** Asked whether
anything became *less* supported than the evidence — the direction this
repository keeps missing — it found **nothing**.

## The `content-reviewer` pass

Ran concurrently with the safety pass, on the same four documents, on a different
dimension. **Both found the gear-requirement defect and the unsourced
keep-and-new premise independently.** The content pass found three more.

**Verdicts:** `core_principles.md` **sound** · `uk_rules.md`, `getting_started.md`,
`body_contact_and_battles.md` **defects found**. **No Critical established.**

⚠️ **The two compressions were both certified SOUND**, by different routes. The
safety pass traced all four `body_contact_and_battles.md` cuts to facts blocks it
read. The content pass decomposed `getting_started.md`'s two removed trailing
paragraphs into **ten** limbs — **five project minutes and five disclosures** —
and found **every disclosure surviving in a voiced layer**: the ASHL price-range
caveat, both drop-in etiquette labels, the EIH session-finder gap, the Board-set
fee, the undocumented paperwork, and the trapezoid *"both were searched"* limb
moved into §1. ⚠️ **They are now better placed than they were, because
`md_to_speech` drops the Sources trailer entirely.** That is the right shape of
compression and it is recorded as such.

### The three further defects

**A cross-reference that is nonsense read aloud.** `uk_rules.md` points at a
heading **whose title contains a comma**, inlined bare mid-sentence, so the comma
re-parses the host sentence and a listener hears *"unisex covers junior
hockey"* — not a proposition about hockey at all. ⚠️ **The repair was necessary
and the replacement is broken:** it read *"The section above…"* until the new U12
section inserted itself between the two.

**An owner's "not sourced" label shed when its claim was promoted into a
standalone-voiced layer.** The owning section opens *"There is no test for this
and no source that gives you one… experience-based judgement, not sourced
guidance."* The new top-of-page bullet carries none of it — and ⚠️ **the claim
GREW in the move**, gaining *"and not hockey sense"* and a both-edges test.
⚠️ **The adjacent line kept its label. One of the two survived the restructure.**

**The corpus narrating its own ownership, in the most prominent sentence of its
highest-hazard document.** `body_contact_and_battles.md` closes its page-opening
principle with *"**Core Principles owns both**"* — non-negotiable 6, and the style
guide names the species exactly. ⚠️ **Second instance in one day:** another agent
caught itself writing *"Core Principles owns this principle"* and rewrote it;
this one reached the tree anyway, in a different document.

### ⚠️ The inversion worth recording

**Two of the defects are the summary layer being RIGHT and the new top-of-page
layer being WRONG.** `getting_started.md`'s Key Takeaway 1 states the
national/local line correctly and names one exception; the new principle paragraph
is a **less careful restatement of the document's own takeaway**. **This corpus's
recorded failure is a correction that reaches the body and stops. This is the
reverse**, and no convention here was watching for it.

### What the pass verified rather than assumed

⚠️ **The new negative-existence claim was attacked a SECOND time, independently,
and held again.** Seven British extractions flattened to punctuation-free,
case-free strings, with a positive control and a deliberate misspelling, and
**every hit read in context**: five are the registration sense (*"icing an
ineligible player"*), one is Rule 76's *"face-offs following icings"*, and the
rest are Appendix D's own U12 provisions. Two reviewers, two searches, same
result.

⚠️ **Appendix D was read LINE BY LINE rather than grepped** — all fourteen U12
figures match the book.

⚠️ **The U12 minor length was checked by LAYER TEST rather than document grep** —
every spoken sentence containing a minute figure extracted from the rendered SSML
and read. **All five sites carry "or four in a four-line game."** The hazard the
brief named — *a listener hearing only "three minutes" returns a minute early* —
**does not occur.** The reviewer called it the cleanest piece of work in the diff.

⚠️ **A `check_caption_echo` hit was read and cleared rather than swept.**
`net-front-walk-out-direction` scores 17.8% against the block a marker moved onto
today, and **the overlap is the repeated safety limb** — one of the three kinds
`CLAUDE.md` names as correct. Recorded so the next round does not rediscover it
as a defect.

### The eighth-principle question, answered independently

**`content-reviewer`: do not promote it**, with reasons the first agent did not
give — it fails the seven's own structural test (four of them honestly answer
*"A realistic alternative: None"*; this one has real alternatives the corpus
already teaches), **the corpus disagrees with itself about its status** across
three documents, and *"the universal rule"* is **an unsourced superlative** of the
same shape as a claim that survived eight review rounds before a primary source
contradicted it. Full reasoning and the constraints-if-promoted are in
`OPEN_ITEMS.md` Workstream 1G.

## The rule-scope propagation wave

### ⚠️ My brief was wrong twice, and the agent refuted both before acting

**FINDING 1 was false.** I relayed that `goaltender.md:516` probably carried the
`defender.md` four-book flattening. It does not — it carries **no gloss at all**
and already splits the divergence correctly across NHL 27.7, IIHF 27.6, USA
Hockey 407(c) and Hockey Canada 4.13(c), with all four parents and Hockey
Canada's Interpretation 2 (*"The determining factor is the position of the
goaltender's skates and not the position of the puck"*) read.

**A whole trap in my brief was inapplicable.** I warned at length about
`check_tables.py` and *"adding a qualifying row is exactly the edit that drops
one."* ⚠️ **`goaltender.md` contains ZERO markdown tables.** There was nothing for
it to bite on.

**And the character counts I relayed were the wrong file's.** 299 and 298 are
`defender.md`'s lines, not this document's.

### The defect the layer test actually found

All **365** facts lines extracted and read alone. **26 matched the flattening
pattern; ONE was a defect** — which is the ratio that makes the *"only reading
decides"* rule worth its cost.

`goaltender.md` said, in body and facts both, that a goaltender may not *"throw
the puck forward **(Rule 67.3(iii))** or participate in the play past the centre
red line — **all four books penalise that**."* The rule number sat unattributed,
joined by *"or"* to a second prohibition, with the four-book claim after both.
**It does not cover both limbs:**

- **NHL / IIHF 67.3(iii)** — a delay-of-game **minor**.
- **USA Hockey 618(c)** — ⚠️ **a FACE-OFF, not a penalty.** USA Hockey's own
  appendix lists it under face-off locations.
- **Hockey Canada** — ⚠️ **no forward-throw clause for a goaltender anywhere**,
  and its Interpretation 6 to Rule 10.2(a) runs the **other way**: *"the Referee
  will stop play, but no penalty will be assessed."*
- **CARHA** — nothing.

**It over-stated the penalty in two books.** Repaired in all three sites, and the
repaired facts line **grew from 264 to 292 characters while gaining two book
scopes** — it had no modal recital to trade away, which is the shape
`CLAUDE.md` records: *"named books beat modal recitals", not "repairs shrink."*

### ⚠️ Twenty-five lines matched the pattern and were NOT defects

Three 67.3 sites naming *"NHL and IIHF Rule 67.3"* and then stating USA Hockey
610(f) and Hockey Canada 10.1(vi) **separately** — verified: Hockey Canada really
does name only *"the top or back of the goal netting"* and really is the only book
without a pads limb. A mask pair where one line's *"all four books"* is **true**
(the delay-of-game floor) and the next states the escalation above it. **A sweep
would have broken every one of them.**

### ⚠️ A near-miss the agent recorded against itself — now a general trap

It read USA Hockey 304(e) and Hockey Canada 3.6(d) with a grep window that
**stopped at a page break**, and was about to report two facts lines as
**fabricated citations**. ⚠️ **The corpus was right and the window was the
artefact.**

Verified by the coordinator: `usah.txt` puts **eight lines of furniture inside a
single rule** — blank lines, a running header with the page number, the InDesign
build line, and the section name — and the rule then resumes. **`grep -A 3` is
unsound on these extractions.** Now recorded in `sources/README.md`.

⚠️ **This is the third member of one family, and all three fail the same way — the
search returns nothing and the file looks fine.** The image-only extraction, the
house-word split, and now the page-furniture split. **A negative-existence claim
is only as good as the search that failed to refute it, and all three break the
search rather than the file.**

### The finding it deliberately did not close

A CARHA-affiliated adult-league goaltender can read `goaltender.md:526`,
correctly conclude there is no trapezoid, and become a third puck-handler behind
their own net — while **CARHA 58(b) bars the smother there on the same two
minutes.** ⚠️ **The section's facts block sits at 11 of 11 facts, the hard cap,
with 8 characters of headroom on the line that would host it.** Putting it in the
body alone creates the body-✓/block-✗ gap the corpus forbids; the remedy is to
split the section, **which is authoring, not a correctness pass.** It is a plan
row rather than a silent half-fix.

### And it flagged its own repair

⚠️ *"A repair is new text, and new text has not been reviewed."* Its own fix
asserts that **Hockey Canada writes no forward-throw clause anywhere** — an
unfalsifiable-by-ordinary-review negative, now in three layers, resting on one
agent's grep. ⚠️ **`check_disclosures.py` does not flag it**: the tool's patterns
do not reach that phrasing, so it is invisible to the one worklist built for this
class.

### The systems wave — ⚠️ FOUR of my brief's premises were false

| I relayed | Actual |
|---|---|
| *"Re-derive every line number, they are **certainly stale**"* | **All five resolved exactly.** Staleness was the wrong worry; my **descriptions** were wrong instead. |
| `neutral_zone_systems:505` lacks CARHA | **False — it was the one file that already HAD it**, with the printed-2020 date and affiliated-leagues-only scope in its trailer. |
| `game_management:1160` cites a superseded edition | **False.** It cites **both** editions with both URLs, says which season each governs, and carries a **68-unit flattened diff** of the two Appendix 1s. |
| `defensive_zone_coverage:691` is *"the same shape"* | **False.** It reads *"Two of the four books print no trapezoid at all"* — self-scoping to the comparison set. |
| CARHA Section One is *"Rules 1–13"*; Hockey Canada Rule 1 *"runs 1.1–1.13"* | **Both wrong by one, the same way.** Rule 14 / 1.14 is Rink Lighting in each. The agent read all of both. |

### The split verdict I asked for: 1 of 6 needed it, 4 correctly absent

⚠️ **And the one that needed it was the file my brief said already had it.**
`neutral_zone_systems.md` names CARHA as a third book with no trapezoid, then
writes: *"No trapezoid is not no restriction, though, and **both books** put one
somewhere else: they let the goalie play the puck out there and not sit on it."*
⚠️ **"Both books" excludes the third book it had just named.** A CARHA reader is
told their goalie may fetch a rim and then explicitly left out of the freeze bar,
so they conclude their goalie may sit on it. **CARHA 58(b) says otherwise.**
Permissive direction.

The four correctly absent all use the **named-book** form and route the reader to
their own league, so no reader reaches *"no trapezoid in North America"*. Under
CARHA the instruction is identical, so a fifth book changes nothing a reader does.
**`game_management` has no trapezoid sentence at all.**

### Three unsourced prevalence claims, each contradicted by its own document

*"the standalone book **a large number of Canadian adult leagues play under**"* —
sitting **three clauses from *"this document has no count for it"***, and
contradicting its own trailer's *"governs CARHA-affiliated leagues only and is not
a general rec-hockey standard."* Twice, word for word, plus *"a great deal of adult
rec hockey runs on it."* No membership figure exists in `carha.txt`. All three
replaced with the corpus's settled scope wording — **shorter in all three cases.**

### Two provenance holes

`neutral_zone_systems.md` and `defensive_zone_coverage.md` each quote **England
Ice Hockey R&R 22.3 verbatim** and rest a negative on both In-House editions — and
at HEAD **neither contained the EIH link or the 2025-26 In-House link anywhere.**
Their four sibling systems documents all carry them.

### ⚠️ A handover that did NOT land, verified by the coordinator

The `goaltender.md` agent named `defensive_zone_coverage.md:713` as its *"single
highest-value handover"* — a four-book Rule 67.3 unit that might carry the same
forward-throw flattening. **It does not.** That bullet cites **`67.3(ii)`, the
*unnecessary freeze* limb**, not `67.3(iii)`'s forward throw, correctly scoped as
*"Rules 63.2(vii), 67.3(ii)"* with a pointer to the owner document and an explicit
disclosure that *"the NHL publishes no interpretation of it."* The unit's
four-book tag comes from the whole bullet discussing four books across 67.2, 67.4,
67.5, 618(a) and 614(b). ⚠️ **A correct handover, correctly hedged as a hypothesis,
that turned out not to apply — and checking it cost one read.**

⚠️ **The message routing it arrived after that agent had already finished.** With
nothing staged the hazard was nil, but it is worth recording: **a mid-wave
handover races the agent it is aimed at.**

### A fragility to know before anyone edits `breakouts.md`

⚠️ **`systems/breakouts.md:898` is at the facts-line cap with ZERO headroom.**
Two more at 299 and three at 298 across the six. **Any edit to that line breaks the
cap.**

## The repair wave

### `body_contact_and_battles.md` — all four findings held, and one was under-counted

⚠️ **The reviewer said the lost IIHF tier survived in "four other places". It is
five** — `:136` facts, `:144` body §1, `:1080` facts §9, `:1093` body §9, and Key
Takeaway 4. **`:169` was the single site of six that had lost it.**

The IIHF tier was **re-verified from primary text rather than taken from the
brief**: Rule 101.1 read in full, top to bottom, with its parent Rule 101 and its
**sibling 101.2** — which shows the major is *not* the top of the ladder either,
so the repair says *"not the ceiling"* rather than naming a maximum. ⚠️ **It would
otherwise have capped a tier while fixing a dropped one.** Cross-checked against
the 2026/27 edition: identical clause for clause, so no edition flag is needed.

**Two drafts were rejected by rendering rather than by reading.** A verbatim quote
of the book's *"one of a"* voiced as *"assesses 'one of a' — a 'Minor penalty (two
minutes)'…"* and was replaced. And *"slow is not the same as safe"* was rejected
because it is the **injury** claim the style guide records as having been wrongly
attached to both limbs of the boards prohibition — **rulebook evidence and injury
evidence are different claims.**

The self-narration repair kept **both anchors byte-for-byte** and replaced the
ownership assertion with a description of the other document's content, on the
model a sibling already uses.

### ⚠️ The caption judgement the echo tool structurally cannot make

`net-front-walk-out-direction` scores 17.8% against the block a marker moved onto
today. **Verdict: leave it, and the decisive reason is invisible to the tool.**
The marker is hosted by **three** documents, and in the other two **there is no
574-word block above it — so the caption is the only place the goal-frame,
spinal-mechanism, USAH 608(b), HC 7.5(c) and skates-down material appears at that
point.** ⚠️ **Trimming it to cure an echo in one document would strip a safety limb
from two others.**

And the caption is **not a subset** of the block: it carries Rule 604(b)'s *"it is
the game in front of you that settles that rather than your age group"*, the
explicit fallback, and a limb addressed to **the other player** — none of which is
in the prose above it.

### ⚠️ A `check_pointers` hit resolved by hand as a FALSE POSITIVE

The tool flagged a mixed document and said in terms that it *"cannot tell which
one the sentence means."* Resolved: the sentence means the `:62` checking table,
which **is** read aloud; the dropped table is `:1126`, and **nothing points at
it.** Recorded so nobody "fixes" it.

### ⚠️ A gap the repair agent flagged against itself — and which was already closed

It wrote: *"I verified the IIHF tier and I did not verify the North American half
of the same sentence… if that compression is wrong, my repair has made the
paragraph MORE symmetrical around a wrong North American limb, which is worse
than the asymmetry was."*

**That gap is covered, but by a different agent and in a different report** — the
`safety-reviewer` verified USA Hockey 604(c)/(d)/(e) and Hockey Canada 7.3(a)/(b)/(c)
against `usah.txt:3555-3600` and `hc_layout.txt` and found them sound. ⚠️ **Neither
agent could know that, and only the coordinator holding both reports can join
them.** That is the handover work the method exists for.

## The `source-verifier` pass — and the finding that outranks everything else this round

**Thirteen URLs fetched with a browser user-agent, all 200, zero unreachable,
zero soft-404s. Forty-one quotations located. Zero not located.**

### ⚠️⚠️ Two of the four IHUK regulations were silently replaced at unchanged URLs — and rolled BACKWARDS

The brief asked it to confirm **one** SHA-256 claim. Doing so meant fetching all
four, and **two no longer match the copies on disk:**

| Document | disk bytes | live bytes | disk `ModDate` | live `ModDate` |
|---|---:|---:|---|---|
| **NIHL 1 & 2 RoC** | 325,259 | **542,722** | **1 Sep 11:29** | **26 Aug 17:53** |
| **WNIHL RoC** | 540,666 | **431,890** | **7 Sep 19:03** | **26 Aug 17:23** |

⚠️⚠️ **The live `ModDate` is EARLIER than the on-disk copy's on both. England Ice
Hockey rolled back to the 26 August originals.**

**So `sources/README.md`'s standing instruction — *"re-fetch and compare dates"* —
was not enough.** A date comparison tells you the files differ; **it does not tell
you which is current**, and the newer-looking copy may be the withdrawn one. The
README now says so, with the measured table and the practical rule: **a SHA-256
against the on-disk copy is the only sound check, and a mismatch is a question
rather than a verdict.**

⚠️ **No corpus quotation is affected — and that was CHECKED, not assumed.** Both
checking-format rows are verbatim in **both** revisions of **both** documents.

⚠️ **This is the second demonstration in six days of a warning that had been
theoretical since it was written.**

### The self-assertion, tested and upheld

`core_principles.md` §2 asserts *"Every quotation above is the governing
document's own wording, and each one is named."* ⚠️ **A document that asserts a
property of itself has made that property checkable.** Tested against **all
eleven** quotations — USA Hockey 604(a) ×2, Hockey Canada 7.3, three IHUK
regulations, IIHF 56.1, USA Hockey's Standard of Play, CARHA 66(a) Note 2, 49(a),
49(c). **Eleven of eleven located and named. Upheld.**

And the checks that usually fail were made and passed: one truncation stops before
an *encouragement*, not a scoping clause; another stops before an injury limb the
corpus **states in its own prose immediately before the quote**; a third's single
quotation marks are ordinary typographic nesting, not drift.

### ⚠️ Three quotation-drift defects, refuting an earlier "all fourteen verbatim"

A `safety-reviewer` had reported all fourteen Appendix D quotations verbatim.
**Refuted on three counts** — an added article, and **two capitals silently
lowered inside the quotation marks.** ⚠️ **The convention they violate is the
document's own:** three lines below, the same section writes
*"[a]ll Under 10 games…"* correctly.

Plus two **paraphrase**-scope slips outside the quote marks, which no checker can
see: ⚠️ ***"Coaches file a line sheet 30 minutes before the face-off"* against the
rule's *"no later than 30 minutes"* — a FLOOR rendered as a POINT IN TIME.** A
reader filing at 25 minutes is late under the rule and on time under the sentence.
And *"the same two lines cover a frozen puck"* is **an inference from a heading**,
presented as the appendix's provision.

### A restater correctly disclosed, and where the disclosure does not reach

The *"walking speed"* finding is **Tator's**, restated by USA Hockey — the source
page carries *"According to research done among a wide range of hockey players"*
and **the very next line is the citation.** ⚠️ **The corpus already discloses this
well, and the verifier confirmed the disclosure is VOICED.** What remains is that
**Key Takeaway 9 travels alone** with *"USA Hockey says"* and the restater note
several chunks away — the mildest form of the class, and *"USA Hockey says it"* is
literally true.

### A near-miss the verifier recorded against itself

It nearly reported `"re-entering"` as drift, because the plain extraction renders
it `reentering`. ⚠️ **It then established that the document contains only FOUR
line-end hyphens in 32 pages and all four fall at genuine compounds — so Word
auto-hyphenation is off and the hyphen is typed.** **The corpus's hyphen is the
author's.** That is the difference between a defect report and a measurement.

## The `facts-reviewer` pass on `defender.md` — two criticals

**All 215 facts in all 28 blocks read, each one alone**, plus
`core_principles.md` (which has no ` ```facts ` blocks — its extraction layer is
`## Key focus` and `## Key Takeaways`, reviewed under the same tests).

**Critical 1.** The `### Icing` block voiced the shorthanded exemption in one
fact, and **that fact cited USA Hockey 624(b)(1) bare** — the sub-clause that
**opens** *"(For all Youth 15-Only and above, Girls 16U and above, High School and
Adult classifications only)"*. **It cited the rule that limits it, without the
limit.** A corpus-wide layer test found **twelve blocks** voicing the exemption;
**this was the only bare one.**

**Critical 2.** ⚠️ **Created that same day by the repair that fixed the first
defect in that file.** The CARHA addition reached the **permission** fact and
**neither restriction** fact, so a CARHA listener heard *"…and CARHA do not [have
a trapezoid] — the dead rim is your goalie's"*, then heard the freezing bar
attributed to two **other** books. **CARHA 58(b) is a minor.** `CLAUDE.md` names
this shape: *"extracted the true half of a two-part rule."*

**Plus four majors** — an unscoped `Never:` contradicted by the same document
eleven sections earlier; a bare permission that the renderer **splits** so the
limit arrives two spoken sentences later; the intent divergence dropped from the
facts layer; and the rule that bites in a *checking* league left as the only
uncited clause in a block of seven citations about the reader's **other** case.

⚠️ **Twenty-six facts matched the "all four books" flattening pattern and only ONE
was a defect.** That ratio is what makes *"a layer test finds candidates; only
reading decides"* worth its cost.

## The `defender.md` criticals — and a coordinator error that would have failed the gate

### ⚠️⚠️ I told the agent `Rule:` facts are exempt from the 11-fact hard cap. THEY ARE NOT.

My brief said: *"The block holds 12 facts, which is under the cap only because
`Rule:` facts are exempt from the coaching cap — so a NEW `Rule:` fact is
permitted by the style guide and is the cheaper repair."*

**Both halves were wrong.** The block held **11**, not 12. And `Rule:` facts are
exempt only from the **8-fact coaching** cap — `scripts/check_facts.py:225` applies
`HARD_MAX` to `len(body)`, the **total**. The tool's own comment says so in terms:
*"Rulebook facts are mandatory… so they do not count against it. **The hard
ceiling still applies to everything.**"* So does the style guide, at `:812`:
*"`Rule:` facts do not count against that 8-fact coaching cap — **but they DO
count against the 11-fact total below**."*

⚠️ **A new `Rule:` fact would have failed the gate.** The agent read both the tool
and the style guide, refuted the brief, and **split the section instead** — which
was the right remedy and the one my brief had ruled out.

⚠️ **I asserted a rule about a checker I own, in a brief that told the agent to
read the file that states it correctly.** This is `CLAUDE.md`'s *"the briefs are
more defective than the corpus"* in its most literal form, and the only thing that
caught it was the standing instruction to refute the brief.

**Four further headroom figures I relayed were understated** — `:633` was 21 not
15, `:605` 24 not 18, `:20` 13 not 7. ⚠️ **The headroom TABLE I passed on
reproduced exactly; the INLINE figures did not.** The table had been measured; the
prose around it had not.

### Both criticals fixed by splitting, not trimming

**`### Icing`** split on the seam the body already uses: the change/time-out half
stays, and a new `#### On the kill` carries the shorthanded half with **the
classification limb as its own fact in the same block** — the construction the six
sibling documents use. ⚠️ **And the agent found a SECOND instance of the same
defect** the brief had not named: the released-before-expiry claim also cited USA
Hockey 624(b)(1) unscoped.

**`#### The trapezoid`** split into *which books paint one* and *what it changes
about your retrievals*, with CARHA 37(e) and 58(b) landing beside the permission
that names CARHA. **`:121` was not edited to fit.**

**Three anchors created or moved; inbound links censused across `content/`,
`project/`, `site/`, `docs/` and `scripts/` — zero.**

### Extract-never-author, observed rather than asserted

To scope the over-the-glass `Never:`, the agent **amended the section's own body
first** so the fact is *extracted* rather than *authored*. That is the discipline
working in the direction it is meant to.

### Three defects found only by rendering

Two of its own new facts opened on dangling antecedents — *"That intent word is
not decoration"* and *"…stops your goalie in the same two places"* — **the exact
species `CLAUDE.md` records**, invisible on the page and broken when voiced alone.
And the renderer turned *"pre-2014"* into *"pre-twenty fourteen"*.

⚠️ **It also fixed a PRE-EXISTING dangling antecedent that the 215-fact review did
not report** — which is the agent's own evidence that **the class is not
exhausted.**

## `uk_rules.md` / `core_principles.md` — and a finding of mine that was backwards

### ⚠️⚠️ The "one finger" finding was REFUTED, and the defect was the note that produced it

My brief relayed that the one-finger fit test is **USA Hockey's Casebook's**, that
the 2.5 cm is British, and that *"the only layer that scopes it is the one the
renderer drops."*

⚠️ **That is backwards. All three British documents print the one finger
themselves, in the same parenthesis as the measurement:**

- In-House Rules **9.8** (both editions): *"The gap between chin/neck and chin
  strap must not exceed 2.5 cm **(approx. one finger)**."*
- England Ice Hockey R&R **24.4**: *"The maximum gap allowed… is 2.5cm
  **(roughly one finger)**."*

**The body, the Common Mistake and Key Takeaway 4 were all correct and needed
nothing.** The defect was a sentence in `## Notes on verification` reading *"Only
USA Hockey's Casebook attaches a fit test — the one finger"*, **which is false —
and which is what produced my brief's finding.**

⚠️ **A note that misleads a reviewer into proposing a scope-label on a correct
sentence is itself the defect.** The agent repaired the note, not the body.

### Two findings were larger than I stated

**The age-token trap was EIGHT defects, not two** — six article-disagreements
(*"A U12 minor"* → *"A under twelve minor"*) **plus two instances of the style
guide's Shape 1, the preposition doubling, which my brief did not mention at
all**: *"Nothing above U12"* → *"above under twelve"*, and *"below U14"* →
*"below under fourteen"*. All recast rather than article-swapped; a post-edit scan
returns zero of both shapes in both files.

**The broken cross-references were FOUR, not one.** Beyond the one I named, `:367`
rendered *"a different tier, and section A first major can end your game, **and
adults are not exempt says so**"*, and `:351` *"…at section under twelve is a
different game, **and neither rule book says so for the junior formats**."*
Repaired by moving each comma-bearing heading into a trailing parenthetical —
**not** by renaming headings, which would have broken a site table of contents the
agent does not own.

**And one item I flagged was not a defect:** a reference I called a silent
truncation is the author's deliberate short form, and **the short form is what
makes the host sentence parse** — the full heading would give *"…at the rink, and
what to ask for, and ask your club and league."* Left alone.

### ⚠️ The diff nobody had done: THEY AGREE

`core_principles.md` §2 and `uk_rules.md`'s U12/U10 section are near-duplicates in
substance, **written by different agents on the same day.** Eight overlapping
claims compared clause by clause — the two silent rule books, the four Rules of
Competition, the U10/U12/U14/U16/U19 lines, NIHL Division 1 & 2 checking, WNIHL
non-checking at both levels, the U10 rule-not-format quotation,
unisex-by-birth-year, and IIHF 101.1 as the only category-scoped checking rule.

**No contradiction, in either layer.** ⚠️ **Two asymmetries found and closed:** one
document's exclusion list omitted **NIHL National** where the other names it — and
where the *same* document names it three times elsewhere — and the currency
warning one voiced and the other did not.

### The restater repair, made minimally

Key Takeaway 9's tail now attributes the walking-speed finding to *"research
restated by USA Hockey"*. ⚠️ **Six words, structure intact.** The agent left the
body's `:179` alone **deliberately**: it *quotes* USA Hockey verbatim, and USA
Hockey is exactly who wrote those words — **changing it would risk implying the
quoted string is Tator's.** That is the distinction the class turns on.

## ⚠️ Quotation drift — a corpus-wide class found at the very end, and a new tool

The last agent of the round was sent to fix **two** quotation drifts in
`body_contact_and_battles.md`. It fixed them, then checked the rest of the
document systematically and **found 47 more.**

It built a comparator to do it, and the technique is the reason it worked:
**normalise both the quotation and every `sources/*.txt` to alphanumerics only,
find the match, map back to the ORIGINAL character offsets, and compare those.**
⚠️ **Matching on alphanumerics defeats the page-furniture splice** — the same
splice that nearly cost two other agents a false "fabricated citation" report this
round — **while still letting the comparison see case and punctuation.** A phrase
grep finds neither the quotation nor the drift.

**Promoted to `scripts/check_quote_drift.py` and named in `CLAUDE.md`.**
⚠️ **Both edits made, and `ls scripts/*.py` checked against that block** — which is
the step `CLAUDE.md` records going wrong twice, because adding a tool and naming
it are separate edits to separate files and nothing connects them. All 24 scripts
are named.

**Measured, three further documents, one pass each:**

| Document | clean | flagged |
|---|---:|---:|
| `foundation/rules_primer.md` | 1,187 | **106** |
| `positions/goaltender.md` | 660 | **29** |
| `foundation/uk_rules.md` | 124 | **8** |

⚠️ **Every gate passes on all of it.** The Markdown is valid and the claim is true;
only the characters inside the quotation marks are wrong. **A `safety-reviewer`
had read fourteen quotations in one section and reported every one verbatim —
three had drifted.**

**The shape that changes meaning** is a terminal period added inside the quote
marks where the source **continues**, presenting a subordinate opener as a
complete sentence. The worst instance quoted *"if at all possible."* where the
source reads *"If at all possible, try to turn skates parallel to the boards
before contact is made"* — **the timing was gone and the fragment read as the
whole instruction.** The repair quoted the whole teaching point rather than
mending the fragment, because a subordinate opener cannot honestly carry a
sentence-final stop either way.

⚠️⚠️ **And the tool's blind spot is the more dangerous half: it cannot see
ATTRIBUTION drift.** It keeps the closest match across **all** sources, so **a
sentence the corpus credits to the NHL but carrying the IIHF's wording scores
clean.** Nothing checks a quotation against the book the prose names, and there is
no tool for it.

⚠️ **Nor does it test whether a verbatim quotation is the RIGHT one.** The agent
named the case to start from: one site truncates before *"or for the purpose of
starting an altercation"* while **the same document quotes the fuller form twelve
lines earlier.**

**The `notfound` column is not a defect count.** Every one checked by hand was
verbatim — page-furniture splices, or quotations whose source is not on disk.
⚠️ **Roughly two dozen quotations in that one document come from the open web and
are neither verified nor flagged: they are invisible.**

## Measurements

| | before | after |
|---|---:|---:|
| markers arriving with zero prose | 28 | **16** |
| `body_contact_and_battles.md` old-side markers | 8 of 8 | **0 of 8** |
| `rules_primer.md` old-side markers | 6 of 9 | 2 of 9 |
| documents reading `already complies` | 20 of 32 | 22 of 32 |
| facts blocks / facts | 834 / 5,602 | 836 / 5,609 |
| corpus words (`check_counts`) | 1,262,521 | 1,266,801 |

**The corpus grew by ~4,300 words on a round whose owner instruction is to make it
shorter — and that is the correct outcome.** Every compression in the round was
verified sound by a reviewer; the growth is scope, qualifiers and citations that
were missing. A U12 playing code, a classification limb, a book's countervailing
restriction, two page-opening principles and four restored rule tiers all cost
words. ⚠️ **A required condition beats a length target**, and this round is the
measurement of what that costs.

## Gate run — on a quiet tree, after every agent finished

| Gate | Result |
|---|---|
| `check_links.py --quiet` | exit 0, silent |
| `check_facts.py` | 26 documents · 836 blocks · 5,613 facts — all conform |
| `check_absolutes.py` | 39 documents + **408 caption/`describe` units** — exit 0 |
| `check_geometry.py` | 77 assertions over 19 named positions — agrees with `rink_map.md` |
| `check_secrets.py` | 334 tracked files, no findings |
| `check_counts.py` | 1,267,108 words — every live corpus-size figure matches |
| `check-arrivals.mjs` | 204 diagrams · 304 routes · **0 hard**, 8 advisory |

⚠️ **The diagrams were rebuilt immediately before this run**, so
`check_absolutes.py` read the current caption layer rather than a superseded one —
the failure `CLAUDE.md` records as covering ten-plus rewritten captions. Its 408
units are real.

⚠️ **This is the run that counts.** Every earlier pass in this round was taken
while agents were live, and `CLAUDE.md` is explicit that such a pass proves
nothing, because the tree it read is already gone.

⚠️⚠️ **AND THIS TABLE WAS ITSELF STALE UNDER THAT SENTENCE.** It carried
*"5,609 facts"* and *"329 tracked files"* — the figures from the run **before** the
split-defect repair and the four new records landed. **A commit gate caught it: a
finality claim over figures that no longer described the tree.** The numbers above
are now the post-repair run, taken on a quiet tree after every agent stopped and
after the rebuild.

⚠️ **The lesson is the round's own, committed inside the round's own record, for
the fourth time: a figure written beside the words "this is the run that counts"
is the single most misleading place a stale figure can sit** — the next
reconstruction credits that table with covering text it never read.

## ⚠️ THE COMMIT GATE BLOCKED, AND BOTH SPLITS HAD CREATED A DEFECT

The gate invoked `review_process.md`'s terminating rule — *"A repair that moves,
merges, splits or renumbers text… does void it, and needs a fresh pass"* — because
both criticals had been repaired by **splitting sections** and the only post-split
reading was by the authoring agent.

⚠️⚠️ **A fresh `facts-reviewer` and a fresh `rules-verifier` then each found a
defect THE SPLIT HAD CREATED. Two splits, two reviewers, one defect each.**

**The trapezoid split severed a permission from two of its three restrictions.**
Before the split one block held, adjacent: the permission, then USA Hockey 614(c),
then Hockey Canada's Interpretation 3. **That adjacency WAS the original repair.**
After it, the permission named three books and the only restriction beside it was
CARHA's — the other two were **26 and 29 spoken sentences away, across a heading**.
A USA Hockey listener would hear the permission, then the freezing bar attributed
to CARHA alone, and conclude their own book had none.

**The icing split dropped the condition that governs an absolute.** `:654` said a
clear released before your penalty expires is *"never icing, however late it
lands."* All four books condition the exemption on being **below the opponent's
on-ice strength at the instant of the shot** — not on a penalty running. **So
"never" fails at 4-on-4 with your own penalty still going.** The old single line
carried the count test **inside the same spoken unit**; the split moved it into a
neighbouring fact and rescoped it to *"After expiry"*.

**Both closed.** The permission and its restrictions are now **two spoken sentences
apart in one block, no heading between** (236 → 238). The icing block was
restructured to lead with *"The exemption is a count, not a clock."*

## ⚠️ Six briefed findings refuted, two of which would have introduced defects

**My brief said "fix both or neither", naming USA Hockey 614(c)(1) as parallel to
CARHA 58(b).** Verified at the character level: **614(a)**, the *skater* clause,
reads *"who **deliberately** falls on or gathers"* — but **614(c)**, the
*goalkeeper* clause, reads *"who falls on or gathers"*, **with no intent word at
all.** ⚠️ **Fixing it would have added a word USA Hockey does not write, and
614(a) is the near-miss that would have "confirmed" the brief** to anyone reading
one clause up.

**My brief called the 2014-15 trapezoid date "unverifiable from anything on
disk."** It is sourced — in its owner, `rink_map.md`, with the reason and the
NHL's own rule-change announcement in the trailer. ⚠️ **I had conflated *"absent
from the rulebook"* with *"unsourced"*.** And *"the NHL rulebook dates no rule"* is
itself false: **NHL 9.7 reads *"Beginning with the 2013-2014 season."*** The agent
caught that one in its own draft.

Also refuted: a census figure (19 uses, not 17, and **two** lacking the anchor, not
one), a spoken distance, and an anchor I attributed to the wrong file — ⚠️ **there
are ZERO fragment links into `defender.md` anywhere in the corpus**, confirmed
independently by the site review.

⚠️ **And the agent caught two defects in its own drafts before shipping them** —
one by re-reading, one by `check_disclosures`. **A repair is new text, and this is
what reviewing your own new text looks like when it works.**

## The `site-reviewer` pass — both blocked items sound on the page

All 49 sitemap pages, 320/375/768/1440, both themes, both `prefers-color-scheme`
and explicit `data-theme`. ⚠️ **The Chrome extension refused every navigation, so
it drove Chrome directly over CDP** — real rendered pixels, but no recorded
walkthrough and no accessibility tree.

**The repaired polygon:** label clear of the boards by **80.7 px at 1440, 75.3 at
768, 65.2 at 375**; zero overlaps against all five SVG text nodes; polygon reaching
the board **to 0.0 px**; both faceoff circles enclosed. ⚠️ **`getComputedStyle(polygon).stroke`
is `"none"` at all six cells — the leftover `stroke-dasharray` is INERT**, with no
dashes in any screenshot, the PNG or the PDF.

**`defender.md`'s new structure:** three anchors resolving at ~128 px, clearing
**both** sticky elements — the header **and the audio player**, which a naive check
would have missed. Facts blocks render as facts panels. Console **clean across all
49 pages: 0 errors, 0 warnings**.

**The CSS gap renders**, measured two ways at three viewports: **30 markers** now
land immediately before a heading or `hr` across 20 pages — h3 44 px, h4 40 px,
hr 44 px — and **the gap above the figure is 32 px against 44 below**, so the
figure binds upward to the prose it illustrates. ⚠️ **`global.css` claimed "all
eleven live cases"; the marker wave made it 30.** Corrected, with the method rather
than a new number.

### ⚠️ The finding no source-level review could have made

**On the page-opening principles, the reviewer's verdict is that the divergence
reads as an ACCIDENT — and the table of contents is where you see it.**
`rules_primer.md`'s heading **is** the principle and works as a ToC entry.
`getting_started.md`'s `## The principle` is **a label with no information in it**.
⚠️ **`body_contact_and_battles.md`'s principle appears NOWHERE in the ToC**, and
inside its Overview the bold lead renders at **the same 17 px and the same weight**
as the three ordinary bold leads following it in the same section — **no
typographic signal that it is doing a different job**, in the document where the
framing principle carries the most safety weight.

**Two are variants of one convention; the third is the convention not applied.**
That turns an open coordinator decision into a defect with evidence.

## Dimension coverage — D1 to D15

⚠️ **A commit gate blocked because this statement did not exist.**
`review_process.md`: *"A review is complete when every dimension has either been
checked or been explicitly declared out of scope in the coverage statement.
Silence on a dimension is the failure mode; it is what let round 9 look clean."*

| # | Dimension | Status |
|---|---|---|
| **D1** | Rules accuracy | **Checked.** `rules-verifier` read NHL 81.6 and 20.3, IIHF 81.6 (both editions), USA Hockey 624, Hockey Canada 6.7 and CARHA 65 end to end. Two later waves re-derived CARHA 37(e)/58(b), USAH 407(c)/614(c)/618(c)/624(b)(1), HC 4.13(c)/10.2(a), NHL 27.7/27.8, IIHF 27.6/27.7/101.1 from primary text. |
| **D2** | Rules travelling without exceptions | **Checked, and it is this round's headline.** The shorthanded exemption was bare in one block of twelve. Two further instances found in `rules_primer.md`. |
| **D3** | Rule-set divergence | **Checked.** The centre-red-line gloss, the forward-throw limb, the over-the-glass minor, IIHF 101.1's ceiling and the CARHA trapezoid scope were each re-scoped from primary text. |
| **D4** | Citation integrity | **Checked.** `source-verifier` fetched 13 URLs, all 200, zero soft-404s, 41 quotations located, zero not located. |
| **D5** | Provenance | **Checked.** The Tator restater question resolved; `core_principles.md` §2's self-assertion tested against all eleven quotations and upheld. ⚠️ **Scope limit: only `uk_rules.md` and `core_principles.md` were refetched.** No other document's citations were re-verified this round. |
| **D6** | Negative existence claims | **Checked.** The U10 icing/offside claim attacked **twice independently**, hit by hit, and upheld both times. The Hockey Canada forward-throw negative attacked **four** times — by the repairing agent, by two later passes, and by the commit gate, which established that `hc.txt` 6.12(d) is the goal-netting clause and not a forward throw. **Upheld every time.** |
| **D7** | The cardinal rule | **Checked.** `content-reviewer` judged all three new page-opening principles against it; all three pass. |
| **D8** | Numeric ownership | **Checked.** The U12 minute figures verified in every layer by layer test. ⚠️ **And breached by the coordinator three times in this round's own briefs** — a block size reported as a defect extent, four headroom figures, and an "every level" count. All corrected. |
| **D9** | The summary layer | **Checked.** ⚠️ **Two defects ran the UNUSUAL way — the summary layer was right and the new top-of-page layer was wrong.** |
| **D10** | The key-facts layer | **Checked.** `facts-reviewer` read **all 215 facts in `defender.md`'s 28 blocks**, each alone, and `core_principles.md` (which has none — its extraction layer is `## Key focus` plus `## Key Takeaways`). Returned **two criticals and four majors**. ⚠️ **A fresh `facts-reviewer` pass over the split blocks then ran**, because a split voids the clearance — **and found a critical the split had created.** Repaired, and the repair re-audited. |
| **D11** | Reader safety | **Checked.** `safety-reviewer` on four documents against eight rulebooks, with the spoken layer rendered rather than reasoned about. Two Majors, both in text written that day, both permissive. |
| **D12** | Read-aloud integrity | **Checked, and it produced more defects than any other dimension** — 8 age-token doublings, 4 broken cross-references, 3 dangling antecedents, 2 chunk-split orphans. ⚠️ **Every one is invisible to every gate and was found only by rendering.** |
| **D13** | Folklore | **Checked.** Three unsourced prevalence claims about CARHA's reach found and replaced, each contradicted by its own document's trailer. Two further caught by their own authors mid-draft. ⚠️ **Not swept corpus-wide** — what was found was met, not surveyed. |
| **D14** | Structure, style, terminology, cross-links | **Checked.** `check_links.py` clean; the site build's own checker resolved 10,747 internal links with anchors. Self-narration found and repaired in the most prominent sentence of the highest-hazard document. |
| **D15** | The rendered site | **Checked.** `site-reviewer` drove real Chrome over all 49 sitemap pages at 320/375/768/1440 in both themes. Both items the gate blocked on render correctly, measured rather than inferred. Console clean: 0 errors, 0 warnings, 49 pages. ⚠️ **Scope limits it declared itself: no Safari, no Firefox, no real phone, NO REAL SCREEN READER** — *"reading order is correct for a screen reader" is an inference from DOM order, not an observation* — **`backdrop-filter` probably did not composite under headless**, and **the PNG cache was not audited beyond the one diagram** (a hash-identical `public/` and `dist/` proves the copy, not the render). |

## What this method could not have found

**No browser was opened.** Whether a marker landing immediately before a heading
reads as separated at 44 px, at 375 px and at desktop, in both themes, is
unverified. `site-reviewer` has not run.

**No audio was rendered or heard.** Moving eight markers in one document moves
eight captions and repacks every chunk after them; a `Rule:` or `Never:` value
that was mid-chunk may now be chunk-terminal. The agent reported it did not diff
`check_chunk_tails` against a pre-move run.

⚠️ **This paragraph said *"No `source-verifier` ran. `core_principles.md`'s
citations have never been refetched by one."* That was true when it was written
and FALSE by the time this record was staged** — a `source-verifier` was
dispatched later the same day, fetched thirteen URLs, and tested that document's
§2 self-assertion against all eleven of its quotations. Its results are recorded
above. **A commit gate caught the contradiction between two sections of one
file.** The round's own lesson, committed inside the round's own record: a
statement of what has not been done goes stale the moment it is done, and nothing
connects the two paragraphs.

⚠️ **Compressing `body_contact_and_battles.md` removed duplicate copies of rule
quotations, which makes the surviving single copies more load-bearing than they
were — there is no longer a second copy to cross-check against.** A
`rules-verifier` pass on NHL 61.1, USA Hockey 623 and Hockey Canada 9.3 as they
now stand is owed.

**Six documents were being written while other agents read them.** Several
findings are flagged as resting on a live file and must be re-derived.

**633 disclosures remain untested**, 70 of them in the ` ```facts ` layer, which
is voiced alone with a 300 ms break either side. Two agents named this as the
highest-yield work left in their own files.

**The census fix has not been made.** `scripts/` is shared state and agents were
live; `CLAUDE.md` forbids changing a tool mid-wave. **Every summary-layer figure
in every plan document is wrong until it lands.**
