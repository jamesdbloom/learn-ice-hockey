/**
 * Diagrams for content/foundation/on_ice_communication.md.
 *
 * The section is the source of truth. Every player below is one its prose places,
 * and where the prose hedges, the caption hedges.
 *
 * WHY ONE DIAGRAM IN A DOCUMENT WITH FIFTY-ODD CALLS IN IT.
 *
 * This document is an INDEX OF CALLS. Almost every call it lists names a play that
 * some other document owns and has already drawn, and a second drawing of the same
 * play is a second place a correction has to reach. Checked, call by call, before
 * writing anything:
 *
 *   "wheel", "reverse", "rim", "up", "over",
 *   "D-to-D", "stretch" .......... `breakout-wheel`, `breakout-reverse`,
 *                                  `breakout-rim`, `breakout-up`, `breakout-d-to-d`,
 *                                  `breakout-stretch-pass` (breakouts.md), plus
 *                                  `winger-dz-rim` and `winger-dz-reverse`.
 *   "over" / "D-to-D", the route . `defender-d-to-d-behind-the-net`. BORROWED into
 *                                  the "Puck direction" section: its caption states
 *                                  this document's own `Never:` — behind the net,
 *                                  below the goal line, never across the slot — and
 *                                  it draws the shaded slot the pass avoids.
 *   "deep" / "dump" .............. `dump-hard-rim`, `dump-soft-area`,
 *                                  `dump-chip-past`, `dump-and-the-trapezoid`.
 *   "back door", "net front" ..... `scan-the-danger-in-your-own-end` names the back
 *                                  door and draws the tap-in this document's
 *                                  "I thought you had him" mistake describes;
 *                                  `goalie-front-door-back-door` draws it from the
 *                                  goaltender's side. BORROWED, the first of them.
 *   "sag" / "collapse" ........... `dz-collapse-corner`, `dz-collapse-high-slot`.
 *   "screen" ..................... `screen-the-goalie-sightline`, `oz-net-front-screen`.
 *   "odd man", "two-on-one" ...... `rush-2-on-1`, `defender-two-on-one`,
 *                                  `rush-3-on-2-default`.
 *   "backcheck", "pick him up" ... `rush-backcheck-lanes`, `centre-backcheck-middle-lane`.
 *   the goalie's trapezoid ....... `the-trapezoid`. CONSIDERED AND DECLINED, not
 *                                  overlooked. It is a good picture and the shape
 *                                  is one this document leaves entirely to prose —
 *                                  but its caption re-states, in full, the same
 *                                  four-book comparison the surrounding paragraph
 *                                  has just made at length, and a caption is VOICED.
 *                                  A listener would hear the trapezoid rules twice
 *                                  in a row. The header links Rink Map and Glossary,
 *                                  which is where the shape belongs.
 *
 * WHAT IS LEFT, and it is the one thing no other document owns: the INFORMATION
 * ASYMMETRY ITSELF. This document's thesis is stated four times — "the puck carrier
 * is the least informed player on the ice", "the forechecker closing from behind is
 * in the one place they cannot look", "a defenceman retrieving a dumped puck is
 * facing their own end boards with the forecheck coming from behind — the precise
 * view the goalie has and they do not", and "the puck retriever cannot see the
 * forecheck arriving; their partner and the weak-side defenceman can". That is not
 * a play. It is an arrangement of four people in which two hold information and two
 * need it, and the only thing that moves it is a voice.
 *
 * ⚠️ WHY NOT `breakout-five-roles`, WHICH LOOKS LIKE IT ALREADY DOES THIS. Its
 * caption does carry one half — D2 "can see the forecheck arriving behind his
 * partner, which is why the calls are mostly his" — and it was the closest call in
 * the file. It is not the same picture, and its own caption says why: "No forecheck
 * is drawn: this is the shape, not a play against anybody." The forechecker in the
 * blind side is exactly what it omits, deliberately, and it is the whole subject
 * here. It was ALSO not borrowed into this document, because it would put a second
 * picture of the same corner with the same two defencemen on the page teaching the
 * five breakout roles, which is breakouts.md's subject and not this one's.
 *
 * ⚠️ WHAT CANNOT BE DRAWN, recorded so the next author does not spend the afternoon
 * rediscovering it. THE NOTATION HAS NO FACING. Both published keys draw a player as
 * a circle or a triangle with no front and no back — see the header of
 * site/scripts/lib/rink.mjs and the same finding in scanning_and_anticipation.mjs,
 * whose subject has the identical problem. So "cannot see him" is NOT drawn here and
 * cannot be. What is drawn is the geometry it follows from: the retriever's route
 * runs away from the forechecker, and the forechecker's runs at his back. Inventing
 * a sightline arrow, an eye glyph or a shaded "blind side" polygon would be exactly
 * the defect that header records — a symbol whose meaning came from memory rather
 * than from a key. THE CAPTION SAYS SO IN TERMS. (`screen-the-goalie-sightline`
 * shades a sightline legitimately, and the difference is worth stating: that wedge
 * is the straight line between two drawn points, the puck and the goal, so it needs
 * no facing. A blind side needs one.)
 *
 * TWO FURTHER THINGS THE DOCUMENT WANTS AND WERE DECLINED:
 *
 *   The "man on" POSTURE — skates parallel to the wall, forearm and hip, chin off
 *   the chest. It is the document's most safety-critical passage and it is
 *   undrawable for the same reason: a circle has no shoulders and no skates.
 *   content/technique/body_contact_and_battles.md owns the treatment and this
 *   document points at it. The instruction is carried in the CAPTION below instead,
 *   in the document's own words, because a caption is voiced alone and this is the
 *   moment the warning attaches to.
 *
 *   ⚠️ AND IT HAS TO BE THE WHOLE INSTRUCTION, because the caption is voiced ALONE.
 *   It carried only half of it. THREE THINGS WERE MISSING and each is recorded here
 *   so the next edit does not shed them again:
 *     (1) "and never duck". The section's ducking limb lives at :101, in the "heads"
 *         entry, which the speech renderer puts in a DIFFERENT CHUNK from this
 *         caption — so a listener who heard this caption got the boards instruction
 *         with the ducking half missing. Thirteen of the corpus's other
 *         boards-posture captions carry it; this was the one that did not.
 *     (2) THE TWO LIMBS, kept apart. The caption joined the cervical-flexion finding
 *         to the turned-back prohibition with an em-dash. Three owner documents
 *         forbid exactly that join in terms — content/technique/puck_handling.md and
 *         content/technique/body_contact_and_battles.md, whose Sources say it
 *         plainly: "the walking-speed finding is about a head-down, head-first
 *         collision, and USA Hockey does not say it about turning your back." The
 *         turned back's own hazard is that it makes the contact a hit from behind,
 *         and the sentence for THAT is Introduction To Body Contact §5: "Even a
 *         light hit from behind could inflict a severe head or neck injury." Both
 *         strings re-located in sources/huh.txt and sources/ibc.txt this session.
 *     (3) A LEVEL CAVEAT. The picture is a checking-from-behind fact pattern drawn,
 *         and the caption told a reader how to take a check without telling them
 *         whether they may be checked at all. Rule text checked against sources/
 *         this session rather than carried in: USAH 604(a) "Body checking is
 *         prohibited in the 12 & under youth age classifications and below, all
 *         Girls'/Women's age classifications and all non-check Adult
 *         classifications"; HC 7.3's opening "This rule applies only in divisions of
 *         U13 and below, in female hockey, and any other divisions approved by a
 *         Member of Hockey Canada"; IIHF 101.1 bodychecking in Women's Hockey
 *         "allowed when there is a clear intention of playing the puck or attempting
 *         to 'gain possession' of the puck with the exception from the situation
 *         described in this rule". ⚠️ THIS QUOTATION USED TO STOP AT "the puck",
 *         one limb short and with the exception qualifier gone, and the caption was
 *         written off it and shipped the same half-rule. A truncated quotation in a
 *         provenance comment is how a half-rule reaches a listener.
 *         `forecheck-pinch` in forechecking_systems.mjs carries the same three.
 *   The checker's-obligation clause the section states at :95 — "the checker carries
 *   the legal obligation here, not you" — is carried too, and deliberately NOT left
 *   as reassurance: a penalty is assessed after the injury has happened.
 *
 *   Hand signals at a faceoff. A glove pointed at a spot, a blade angled at a
 *   target. No key has a symbol for any of it.
 *
 * PERSONNEL IS NOT SHAPE. The published key has no neutral glyph — a circle is a
 * forward and a triangle a defenceman — so every glyph is a personnel claim whether
 * the diagram wants to make one or not. Here the document names them: "a defenceman
 * retrieving a puck in the corner", "their partner and the weak-side defenceman",
 * "the forechecker", "the goalie". Two triangles, one circle, one G.
 *
 * ASSUMES site/src/data/rink.json AS ON DISK IN THIS WORKING TREE, and every
 * clearance stated below dies if one of these moves: faceoff-dot (69, +/-22),
 * top-of-circle (54, +/-22), half-wall (69, +/-38.5), corner (82, +/-34),
 * goal-line (89, 0), crease (86, 0); sheet 200 x 85 with a 28 ft corner radius, so
 * the near corner's arc centre is (72, 14.5). Glyph ink is rink.mjs's: a forward's
 * circle is r 2.9, 3.275 with half its 0.75 outline and 3.875 counting the 1.95
 * halo; a defenceman's triangle reaches 3.6 from the centroid and 4.0 with half its
 * 0.8 round-joined stroke.
 */

const OWNER = 'content/foundation/on_ice_communication.md';

/* ------------------------------------------------------------------ *
 * 1 - The call exists because of who can see it        (section "Why
 *     verbal communication wins games")
 * ------------------------------------------------------------------ */

// THE RETRIEVER. "A defenceman retrieving a puck in the corner" — the section's own
// words, so a TRIANGLE. He has NOT got the puck yet: the section says "retrieving",
// and a player skating hard at his own end boards to collect a loose puck is both
// what the sentence describes and what makes the blind side real. So his route is
// `skate` (plain line, arrowhead) rather than `carry`, and the puck sits ahead of
// him rather than on his stick.
//
// 4 ft up-ice of the right faceoff dot and 4 ft toward the middle. Inside the right
// circle (4.47 ft from its centre), which is where a defenceman crossing to the
// corner actually is.
const D1 = { at: 'faceoff-dot:right', dx: -2, dy: 4 };      // (67, 26)
// Stopped 6.1 ft short of the puck so the 3.15 ft arrowhead is not drawn through
// the puck mark. Route length 13.4 ft, which shows as a line rather than a stub.
// Boards: 18.85 ft from the corner arc's centre (72, 14.5) against a radius of 28,
// so 9.1 ft of ice to the dasher.
const D1_TO = { at: 'corner:right', dx: -3, dy: -2 };       // (79, 32)

// THE FORECHECKER. A solid circle: opposition, and a forward, which is what a
// forechecker is. Twenty feet behind the retriever, just outside the top of the
// strong-side circle and a little toward the middle.
//
// BEHIND IS MEASURED, not eyeballed, because it is the entire claim: the retriever
// travels (67, 26) -> (79, 32), unit (0.894, 0.447); the vector from him to the
// forechecker is (-19, -6.5), whose dot with that unit is -19.9. Negative, and
// nearly the full length of the vector, so the forechecker sits almost exactly on
// the retriever's line extended backwards — which is what "from behind" means and
// what makes the picture legible. Nothing in the DRAWING says the retriever cannot
// see him — see the header — and the caption says so.
//
// ⚠️ FOUND BY RENDERING, and recorded rather than silently fixed. He was first
// drawn on the wall at (50, 34.5), 19 ft away with a dot of -11.4 — behind by the
// arithmetic and NOT behind to the eye, because from there he reads as arriving
// alongside the retriever and angling him to the corner rather than chasing him
// into it. A dot product that clears zero is not the same thing as a picture a
// reader decodes as "behind", and only the render shows the difference.
const F1 = { at: 'top-of-circle:right', dx: -6, dy: -2.5 }; // (48, 19.5)
// `pressure`, which ends in the key's two bars and NOT an arrowhead. Two reasons,
// and the second is the load-bearing one. It finishes on a patch of ice rather than
// on a person, and a bar says arrive and contain where an arrowhead says continue
// through — this notation's own distinction, taught to the reader at
// content/reading-diagrams/reading_ice_hockey_diagrams.md ("a pressure route also
// ends in it"). And it is what keeps the picture out of a contact claim: no contact
// is drawn, this document does not teach checking, and what an arrival like this may
// end in depends on whether the reader's league allows body checking at all.
//
// Measured. The route runs (48, 19.5) -> (60, 24), 12.82 ft, unit (0.936, 0.351).
// The two terminal bars sit 2 ft either side of the tip across travel, at
// (59.30, 25.87) and (60.70, 22.13); the nearer of them is 7.38 ft from the
// retriever's anchor, clearing his triangle's 4.0 ft of ink by 3.38 ft. The tip
// itself is 7.28 ft off him.
const F1_TO = { at: 'faceoff-dot:right', dx: -9, dy: 2 };   // (60, 24)

// THE TWO WHO CAN SEE IT AND CANNOT REACH IT.
//
// The partner, "on the opposite side of the net, level with the goal line" — the
// placement `breakout-five-roles` gives D2 and the reason this document gives for
// the calls being his. A triangle: the section says "their partner and the weak-side
// defenceman". 13 ft off the middle, so clear of the crease (|y| <= 4 within 6 ft of
// the goal line) and of the goal frame (x 89..92.33, |y| <= 3); 10.9 ft of ice to
// the dasher, measured off the far corner's arc centre (72, -14.5).
const D2 = { at: 'goal-line', dy: -13 };                     // (89, -13)
// Our own goaltender. The section: "they face the play, they are stationary, and
// nothing in the zone is behind them."
const G = { at: 'crease', dx: 1 };                           // (87, 0)

// The loose puck in the corner, 6.1 ft beyond the retriever's route tip and 3.8 ft
// of ice from the dasher (24.19 ft from the corner arc's centre, radius 28).
const PUCK = { at: 'corner:right', dx: 2, dy: 1.5 };         // (84, 35.5)

const whoCanSeeIt = {
  id: 'the-call-and-who-can-see-it',
  owner: OWNER,
  half: true,
  width: 900,

  caption:
    'Why the call exists at all: the player going to get the puck is the one who cannot see the ' +
    // ⚠️ "IN THIS PICTURE" IS LOAD-BEARING. Anyone with a view can make the call — the section's
    // own organising principle, and its :58 says flatly "Everyone else can see it" — so a bare
    // "the two who can see it" would be an absolute the document contradicts three times. Only
    // four players are drawn here; this sentence describes them, not hockey.
    'danger, and in this picture the two who can see it are the two who cannot reach it. This is ' +
    'your own end. A ' +
    'defenceman is skating back into the corner for a loose puck; a forechecker is closing on him ' +
    'from behind, which is the one place his eyes do not reach; and his partner across the net and ' +
    'his goaltender can both see both of them and are both too far away to do anything about it ' +
    'except say so. That is the whole mechanism of talking on the ice — the information that would ' +
    'save the retriever is already sitting in somebody else’s eyes, costing nothing, and useless ' +
    'until it is spoken. Say it while the puck is still travelling rather than as it arrives, keep ' +
    'it to a word or two, make it loud enough to survive a rink, and use his name, because an ' +
    'unaddressed shout turns four heads. Where two people can see it, both should call it: the ' +
    'failure being guarded against is silence, not duplication. What the words are is a team matter ' +
    'and not a law of hockey — there is no official hockey vocabulary, and calls vary by team, ' +
    'region, level and coach, so agree your own list and drill it rather than assuming anyone ' +
    'else’s travels. Three honest limits. This notation has no facing, no head and no sightline, so ' +
    'the claim that the retriever cannot see him rests on which way he is travelling and is not ' +
    'something the drawing itself establishes. ' +
    'Which side the forechecker arrives from is one instance rather than a ' +
    'fixed arrangement, and how many are coming is a separate call again — one forechecker can be ' +
    'beaten with a reverse or a quick turn, two means you are being sealed and the puck has to ' +
    'leave. And the mirror of this picture is the same corner with nobody arriving, where the call ' +
    'is "time" and the danger runs the other way, because a false "time" sends a teammate into ' +
    'contact he was not braced for. ⚠️ Which is the safety instruction this picture carries, since ' +
    'the warning is shouted at a player arriving at the wall who cannot see the check coming. Two ' +
    'separate postures injure a neck at the boards, and they are not the same warning. A back ' +
    'turned to the wall is one: it makes the contact a hit from behind, which USA Hockey warns ' +
    'could inflict a severe head or neck injury even when the hit is light. A chin dropped to the ' +
    'chest is the other: it straightens the cervical spine, and USA Hockey’s Heads Up Hockey ' +
    'guide, restating research by Charles Tator and colleagues, has it that almost all on-ice ' +
    'cervical spine injuries have come from the head being slightly flexed while making head-on ' +
    'contact with the boards or the goal post. So never take that contact with your back to the ' +
    'boards, and never duck. ' +
    // ⚠️ THIS SENTENCE NAMES ITS OWN SUBJECT AND RE-CARRIES BOTH PROHIBITIONS ON PURPOSE.
    // The caption is 3,500-odd characters against md_to_speech's MAX_BILLED_CHARS of 2,800, so
    // it CANNOT be voiced in one chunk and `_split_paragraph` cuts it at a sentence boundary
    // near the limit — measured this session, immediately after "and never duck." A listener
    // who hears only the second half would otherwise open on a bare "Get your skates parallel
    // to the wall", an instruction about contact they were never told to expect, which is the
    // dangling-demonstrative defect this same round found at the section's :62. Written this
    // way, either half stands alone with both absolutes in it. If the caption is edited, the
    // cut moves: re-run md_to_speech.transform_document and read the new boundary.
    'The posture that survives an arrival at the wall is skates parallel to it, the contact taken ' +
    'on your forearm and hip, head up and chin off your chest — never your back to the boards, ' +
    'and never a chin dropped to look for the puck. Protecting the puck never justifies squaring ' +
    'your back to an oncoming checker. The checker carries the legal obligation here, not you — but that is ' +
    'not something to lean on, because the penalty arrives after the injury does, and because in ' +
    'many leagues body checking is not allowed at all. Find out which book you are under before ' +
    'you plan on taking contact or making it: USA Hockey Rule 604(a) prohibits body checking at 12 ' +
    '& under and below, in every girls’ and women’s classification and in non-check adult hockey; ' +
    'Hockey Canada Rule 7.3 applies at U13 and below, throughout female hockey and in any other ' +
    // ⚠️ BOTH LIMBS AND THE EXCEPTION, BECAUSE "ONLY" MAKES A TRUNCATION A FALSE
    // PROHIBITION. This shipped as "only where there is a clear intention of playing the
    // puck" — which, with "only" in front of it, tells a woman playing under the IIHF book
    // that a check made while going for the puck she does not yet have is illegal. It is
    // not. IIHF 101.1, verified against sources/iihf_rules.txt this session: "In Women's
    // Hockey 'bodychecking' is allowed when there is a clear intention of playing the puck
    // or attempting to 'gain possession' of the puck with the exception from the situation
    // described in this rule." Two limbs, then a qualifier. The corpus has a settled short
    // form for the qualifier — "subject to the exception the rule itself then sets out",
    // used at forechecking_systems.md:502, defending_the_rush.md:353,
    // offensive_zone_play.md:792 and game_management.md:770 — and center.md:606 and
    // game_management.md:770 carry both limbs. This caption now carries all three parts.
    'division a Member approves; and the IIHF restricts by category rather than by age, allowing ' +
    'bodychecking in women’s hockey only where there is a clear intention of playing the puck or ' +
    'an attempt to gain possession of it, subject to the exception the rule itself then sets out.',

  describe:
    'Your own end, your net at the right and your goaltender in the crease. A loose puck sits in ' +
    'the right corner. One of your defencemen, an open triangle, is between the right faceoff dot ' +
    'and the corner; a plain arrowed route runs from him out toward the corner and stops about six ' +
    'feet short of the puck. An opposition forward, a solid circle, stands about twenty feet ' +
    'behind him, just outside the top of the same faceoff circle and a little toward the middle of ' +
    'the ice, on the line the retriever has just skated along; his route runs after him and ends in ' +
    'two short bars across the line, about seven feet short of him, rather than in an arrowhead. ' +
    'Your second defenceman, an open triangle, stands on the ' +
    'far side of the net level with the goal line, and neither he nor the goaltender has a route: ' +
    'nobody else in the picture moves. Both of them are labelled "calls it", the retriever is ' +
    'labelled "cannot see him" and the forechecker "closing behind him". No contact is drawn, and ' +
    'nothing in the picture shows where anybody is looking.',

  // NO SHADED ZONE, deliberately. A "blind side" polygon would need a facing this
  // notation has not got. See the header.
  players: [
    { id: 'G', pos: 'G', at: G, label: 'calls it' },
    // Triangles, because the document says defencemen: "a defenceman retrieving a
    // puck in the corner", "their partner and the weak-side defenceman".
    { id: 'D1', pos: 'D', at: D1, label: 'cannot see him' },
    { id: 'D2', pos: 'D', at: D2, label: 'calls it' },
    // A circle: a forechecker is a forward. Which of their forwards is not the
    // document's subject and the caption does not make it one.
    // ⚠️ LABEL SHORTENED AFTER RENDERING, and recorded rather than silently changed.
    // "closing from behind" is 19 characters; the placer could not fit its box
    // anywhere near the glyph and put it 25 ft away in the neutral zone on a leader
    // line that crossed the blue line. "closing behind him" is short enough to sit
    // beside him. The wording lost nothing: the caption and `describe` both carry
    // the direction in full.
    // ⚠️ AND THE CHANGE REACHED THIS COMMENT AND STOPPED. `describe` went on naming
    // the OLD label, "closing from behind", for a screen-reader user reading a page
    // whose glyph says "closing behind him" — a label that is not on the picture.
    // Fixed in `describe` above. If this label is ever reworded again, the string in
    // `describe` is the second place it has to change.
    { id: 'F', team: 'opp', pos: 'F', at: F1, label: 'closing behind him' },
  ],

  // NOT `numbered`. Both routes are one instant — the forechecker is arriving while
  // the retriever is still travelling, which is precisely the window the call has to
  // land in. Numbering them would say one preceded the other.
  routes: [
    { from: D1, to: D1_TO, kind: 'skate' },
    { from: F1, to: F1_TO, kind: 'pressure' },
  ],

  puck: PUCK,
};

export default [whoCanSeeIt];
